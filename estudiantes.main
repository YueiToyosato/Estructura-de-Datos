#include <iostream>
#include <string>
#include "estudiantes.h"

using namespace std;
int main()
{
	estudiantes alumno;
		string nombre;
		cout << "Ingrese el nombre ";
		getline(cin, nombre);
		alumno.set_nombre(nombre); //Llamando al metodo set_nombre de la clase estudiantes
		cout << "El nombre del estudiante es: " << alumno.get_nombre();
		return 0;
}
